# ===========================================
# CARDANO OTC DAPP - ENVIRONMENT VARIABLES
# ===========================================
# ⚠️  セキュリティ警告：実際の本番環境では絶対に機密情報をgitにコミットしないでください
# 👉 このファイルをコピーして .env として使用し、実際の値を設定してください

# Cardano Network Configuration
VITE_CARDANO_NETWORK=mainnet

# Blockfrost API Configuration (Frontend)
# 👉 https://blockfrost.io でAPIキーを取得してください
VITE_BLOCKFROST_API_KEY=__REPLACE_WITH_YOUR_BLOCKFROST_API_KEY__
VITE_BLOCKFROST_URL=https://cardano-mainnet.blockfrost.io/api/v0

# OTC Escrow Configuration 
# 👉 実際のエスクローアドレスに置き換えてください
VITE_ESCROW_ADDRESS=__REPLACE_WITH_ACTUAL_ESCROW_ADDRESS__

# Database Configuration (Backend)
# 👉 PostgreSQLデータベースのURL
DATABASE_URL=__REPLACE_WITH_ACTUAL_DATABASE_URL__

# Transaction Submission Configuration
VITE_SUBMIT_BACKEND=wallet

# Security Configuration
VITE_MAX_TTL_MINUTES=2160
VITE_MIN_TTL_MINUTES=5

# Development Configuration
VITE_ENABLE_DEBUG=false
VITE_MOCK_WALLET=false

# Server Configuration  
PORT=3001
HOST=localhost

# 🔐 JWT Secret - 本番環境では強力なランダム文字列を使用
# 👉 openssl rand -base64 32 で生成することを推奨
JWT_SECRET=__REPLACE_WITH_SECURE_RANDOM_JWT_SECRET_AT_LEAST_32_CHARS__

# CORS Configuration
CORS_ORIGIN=http://localhost:4000

# Frontend Configuration
PUBLIC_BASE_URL=http://localhost:4000

# Backend Blockfrost API Configuration (Server-side)
# 👉 サーバーサイド用のBlockfrost APIキー（フロントエンド用と分けることを推奨）
BLOCKFROST_API_KEY=__REPLACE_WITH_ACTUAL_SERVER_BLOCKFROST_API_KEY__
CARDANO_NETWORK=mainnet

# WebSocket Configuration
WEBSOCKET_URL=http://localhost:3001

# 🔐 Redis/Upstash Configuration
# 👉 Upstash Redis (https://upstash.com) またはローカルRedisを使用
KV_REST_API_URL=__REPLACE_WITH_ACTUAL_UPSTASH_KV_URL__
KV_REST_API_TOKEN=__REPLACE_WITH_ACTUAL_UPSTASH_KV_TOKEN__
REDIS_URL=__REPLACE_WITH_ACTUAL_REDIS_URL__
KV_REST_API_READ_ONLY_TOKEN=__REPLACE_WITH_ACTUAL_UPSTASH_READ_ONLY_TOKEN__
KV_URL=__REPLACE_WITH_ACTUAL_UPSTASH_KV_URL_WITH_AUTH__

# 🔐 Encryption Key (AES-256用)
# 👉 本番環境では32バイトのランダムキーを使用
ENCRYPTION_KEY=__REPLACE_WITH_32_BYTE_RANDOM_ENCRYPTION_KEY__

# ===========================================
# 開発環境用の設定例（ローカル開発時のみ使用）
# ===========================================
# ローカルRedis使用時
# REDIS_URL=redis://localhost:6379

# ローカルPostgreSQL使用時  
# DATABASE_URL=postgresql://user:password@localhost:5432/otc_dapp

# ===========================================
# セキュリティのベストプラクティス
# ===========================================
# 1. .envファイルは絶対にgitにコミットしない
# 2. 本番環境では環境変数またはシークレット管理サービスを使用
# 3. APIキーは定期的にローテーションする
# 4. 開発環境と本番環境で異なる認証情報を使用
# 5. JWT_SECRETは十分に複雑なランダム文字列にする